name: pizza_order_app
description: Um aplicativo para pedidos de pizza.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  geolocator: ^10.0.0
  geocoding: ^2.1.0
  permission_handler: ^11.0.0
  flutter_map: ^6.0.0
  provider: ^6.0.5
  sqflite: ^2.3.2
  path: ^1.8.3
  google_maps_flutter: ^2.6.0
  http: ^1.1.0


dev_dependencies:
  flutter_test:
    sdk: flutter
    

flutter:
  uses-material-design: true

  assets:
    - assets/images/


